{
  "name": "@sweez/libs",
  "version": "1.0.9",
  "description": "libraries for sweezey services",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cleanBuild": "rm -r ./build/* && tsc",
    "pub": "npm run cleanBuild && npm version patch && npm publish --access public"
  },
  "author": "Edward Sweezey",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@types/express": "^4.17.15",
    "@types/jsonwebtoken": "^9.0.1",
    "express": "^4.18.2",
    "express-validator": "^6.14.2",
    "jsonwebtoken": "^9.0.0"
  }
}
